version: "3.9"

services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: joobot
      POSTGRES_PASSWORD: joobot
      POSTGRES_DB: joobot
    ports:
      - "5432:5432"
    volumes:
      - joobot_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U joobot"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  joobot_pgdata:
